# Agent Instructions

## Testing

Always update tests after implementing or changing any component, hook, or utility. When the API or UI of a component changes (props, rendered text, structure), update the corresponding test file to match before considering the task complete.
